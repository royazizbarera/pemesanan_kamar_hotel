name: pemesanan_kamar_hotel
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  carousel_slider: ^4.2.1
  cloud_firestore: ^4.16.0
  cupertino_icons: ^1.0.6
  fast_cached_network_image: ^1.2.9
  firebase_auth: ^4.19.0
  firebase_auth_token: ^1.0.5
  firebase_core: ^2.28.0
  flutter:
    sdk: flutter
  http: ^0.13.6
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
